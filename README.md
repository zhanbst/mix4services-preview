# Mix4Services — design preview

Static preview of the redesign. Two variants:

- `index.html` — smooth inertial scrolling, parallax backdrop
- `v2.html` — full-screen sections that snap, each with its own background

Published with GitHub Pages from this repository's root.
